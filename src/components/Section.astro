---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<section class={`py-24 px-6 ${className || ""}`}>
  <div class="max-w-4xl mx-auto">
    <slot />
  </div>
</section>
